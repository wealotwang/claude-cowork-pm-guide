# Day05 学习卡

日期：2026-07-31
主题：把规则模板 + 评测集 + HTML 评估方案，收束成一个最小可运行 demo

---

## 今日核心产出
- 确定了 demo 架构：**正则前置 + LLM判断的混合两层**，不是所有case都无脑上LLM
- 定义了统一输出 schema（matched_category / risk_level / action / rationale / matched_by）
- 实际写出并在沙盒里跑通了代码：
  - `rules_config.json`（规则定义唯一来源）
  - `checker.py`（regex前置层 + LLM判断层 + CLI）
  - `eval_cases.json`（合并自拟case与CRM框架case，约30条，覆盖违规/边界/near-miss/已知局限）
  - `run_eval.py`（按高危/中低危分开算指标的评测脚本）
- 详见：[demo/README.md](./demo/README.md)

---

## 今日关键校准

### 1. 不是所有判断都要交给 LLM
05类（隐私）是结构化、格式固定的信息（身份证号、手机号），正则就能可靠识别，快且不占用API调用；01/07类的判断难点在语义关系（比如"讲课费"本身不违规，关键是有没有和处方量/进院挂钩），这种才真正需要LLM。这个架构选择直接落地了 Day04 悟出的道理，也是能在面试里讲的产品判断——不是"AI能力越强越好"，而是"该用什么方法就用什么方法"。

### 2. API key 不能由我经手，demo要对这件事设计得体面
真实LLM判断需要用户自己的 Anthropic API key，这个值任何情况下都不应该被我输入、存储或上传。所以代码设计成从环境变量读取，用户在自己终端里设置；没设置时脚本要清楚报错、给出解决办法，而不是崩溃或者假装能跑。这也是一个值得记录的工程判断：demo设计要考虑"运行环境不完整时怎么优雅降级"。

### 3. 已知局限要写进代码和文档，不是藏起来
05类正则测试时特意留了一条"已知会漏掉"的case（`05-gap-1`：只提到"电话号码"但没写具体数字），用来证明我们清楚这个demo没覆盖非结构化PII识别，而不是让这个局限被意外发现。评测报告里也把"跳过/未测"和"测过但失败"分开统计，避免指标被没跑的case拉低而失真。

---

## 今日验证结果
沙盒里跑通了 regex 部分（05类），2条可测case，Recall 100%；01/07/10类的LLM部分因为没有API key被正确跳过（不是报错崩溃），报告清楚标注了跳过原因。完整分数要等 Day06 用真实 key 跑。

---

## 明日预告
Day06：用真实 API key 实际跑一遍 `run_eval.py`，记录基线分数（Recall / 误报率），然后做至少1轮"改规则前后对比"的迭代——这是评测驱动方法论最终要在作品集里体现的证据。
