# Day07 荣誉墙

日期：2026-08-06

---

## 今日主题
把 Day07 从“准备做评测驱动迭代”推进到“已经拿到一轮 before / after 证据，并开始用 300 条 benchmark 压测当前 6 类规则”

## 今日完成
- 回忆并校准了当前真正生效的规则口径：
  - `01 / 03 / 05 / 06 / 07 / 10`
- 把旧的 4 类口径和新的 6 类口径正式区分开，避免后续接力继续混淆
- 用同一个模型、同一套思路，完成了 Day06 vs Day07 的第一轮对比：
  - `CSV50` 高风险召回率从 `45%` 提升到 `50%`
  - 报错数从 `3` 降到 `1`
- 跑通了新的 `300 case` benchmark：
  - 总数 `300`
  - 成功跑完 `273`
  - 高风险召回率 `87%`
  - 中风险判断准确率 `96%`
  - 低风险误伤率 `5%`
- 把跑分报告重写成更适合复盘的版本：
  - `Day06`
  - `Day07`
  两个页签分开看
- 把问题案例单独摘出来，后面可以直接围绕这些 case 深聊和调优

## 今日关键词
- 规则口径统一
- before / after
- 300 case 压测
- 问题案例集
- 评测驱动迭代

## 今日成就
你今天做成的最有价值的一件事，不是“又跑了一次分”，而是把这个项目往真正的 AI 产品迭代方式推进了一步：
- 不是只看一个总分
- 而是开始区分：
  - 规则到底配了什么
  - 数据集是怎么设计的
  - 哪些指标达标
  - 哪些 case 值得继续挖
- 这会让你后面在面试里不只是说“我做过一个 demo”
- 而是能说“我真的拿数据集压过、看过问题、能解释为什么没达标、知道下一步该往哪修”

## 还没做完，但方向已经对了
- Day07 还没有结束
- 目前只是完成了：
  - 第一轮规则扩充
  - 第一轮大样本压测
  - 第一轮问题 case 摘取
- 后面最值得继续做的是：
  - 针对 `300 case` 的问题样本继续深挖
  - 挑一类最值当的规则继续优化
  - 再做第二轮前后对比

## 一句话奖励
今天你把 Day07 从“想做评测驱动优化”推进成了“已经开始拥有可复盘、可讲述、可继续迭代的真实证据链”。 

---

## Day07 第二轮荣誉墙（同一天，第二个 session）

### 今日主题
从"看300 case失败样本"一路走到"修了两个真实bug、推翻了一个架构判断、验证30/30全过"

### 今日完成
- 把11条问题case按根因拆成三类，不再笼统地说"分数不够高"
- 顺着 `MB143` 查到底，发现真正的根因是代码里一处regex疏漏，不是LLM判断问题
- 修了两个范围内真实bug：`MB143`（05类误伤）、`MB197`（06/01边界）
- 提出并推动了一次有理有据的架构调整：去掉05类的regex前置层，全系统统一改为纯LLM判断
- 顺手抓到一个稳定性问题（LLM输出偶尔截断），加了重试逻辑
- 修正了一条过时的历史eval case（`07-v-2`）
- 最终验证：MVP子集 **30/30 全部通过，0失败0报错**，包括曾经解决不了的`05-gap-1`这次也稳定通过了

### 今日关键词
- 根因分类
- regex bug排查
- 架构决策
- 输出稳定性
- 评测驱动闭环

### 今日成就
今天最有价值的不是"又修了几个bug"，而是完整走通了一次真正的产品判断链路：从一个具体失败case出发，查到底层原因，发现原因牵涉到产品定位本身，然后做出一次有权衡、有取舍的架构决策——这比"我们选了正则+LLM混合架构"这种静态描述有说服力得多，因为它带着真实的翻转过程：一开始为什么这么设计、后来为什么改、改了付出什么代价、换回了什么。

### 一句话奖励
今天你不只是在"用"评测驱动方法论，你亲手把它走完了一整圈：发现问题→归因→决策→验证，而且中间那个"是不是该有regex"的问题，是你自己问出来的，不是我提醒你的。
