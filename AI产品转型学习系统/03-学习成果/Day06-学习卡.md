# Day06 学习卡

日期：2026-08-05
主题：先完成 demo 预检，再进入真实基线跑分与第一轮评测驱动迭代

---

## 今日定焦
- 今天只搞懂一件事：`当前 demo 能不能进入真实跑分阶段`
- 今天不追求：
  - 在没有真实 API key 的前提下硬做完整分数
  - 提前展开第二轮规则优化
  - 顺手再学新的方法论概念

---

## 今日核心产出
- 把当前学习方式升级为固定 4 步：
  - 开场定焦
  - 主卡推进
  - 费曼复述
  - 隔天回忆
- 新增方法文件：[02-学习互动方式-v2.md](../02-学习计划/02-学习互动方式-v2.md)
- 实际运行了一次 `demo/run_eval.py`，完成 Day06 预检
- 明确了 Day06 的真实阻塞点不是代码崩溃，而是本地尚未配置真实 `ANTHROPIC_API_KEY`

---

## 当前验证结果

### 1. demo 可以正常执行
今天已经实际跑通：

```bash
cd 03-学习成果/demo
python3 run_eval.py
```

脚本没有报错退出，说明：
- `eval_cases.json` 能正常读取
- `checker.py` 和 `run_eval.py` 的链路是通的
- 没有 key 时也会优雅降级，而不是直接崩溃

### 2. 当前已经确认的分数
- 05 类 regex 可测用例：2/2 通过
- 高危类（01/05）当前已测 Recall：100%
- 其余 28 条 01/07/10 case 因缺 `ANTHROPIC_API_KEY` 被正确跳过

这说明：
- 当前 demo 不是“还没法跑”
- 而是“已经能跑到一半，完整跑分只差真实 LLM 环境”

### 3. Day06 的前置条件被进一步讲清了
真正进入完整 Day06，只差两件事：
- 用户自己在本地终端设置真实 `ANTHROPIC_API_KEY`
- 重新运行 `python3 run_eval.py`，把完整基线分数写下来

---

## 今日关键校准

### 1. Day06 不是再学一个概念，而是第一次拿真实结果证明方法
前几天主要在定义：
- 产品边界
- 规则模板
- eval 思路
- demo 架构

到了 Day06，学习重点开始切换：
- 不再只是“我觉得这个设计合理”
- 而是“它在评测集上到底跑出了什么结果”

这一步很重要，因为面试里最有说服力的不是“我想得很完整”，而是“我真的跑过、记过分、做过迭代”。

### 2. 先区分“代码问题”还是“环境问题”，能避免误判
如果今天一上来就说“Day06 失败了”，其实是不准确的。

更准确的表达是：
- 代码链路已通
- regex 部分已验证
- LLM 部分尚未完成真实跑分，因为本地 key 还没配置

这个区分很重要，因为它决定了下一步该改哪里：
- 不是先改代码
- 而是先满足运行前置条件

### 3. 新学习方式对 Day06 特别合适
今天第一次把新方法用在 Day06 上，效果是明显的：
- `定焦` 让今天没有扩散到别的主题
- `主卡` 让问题聚焦在“能不能跑”
- `费曼复述` 让 Day06 的目标更容易讲给别人听
- `隔天回忆` 可以用于明天先复盘：Day06 到底卡在什么地方

---

## 费曼复述
- Day06 是什么：
  - 它是“把前面设计好的 demo 真正跑起来，并记录基线结果”的一天。
- Day06 不是什么：
  - 它不是继续堆新概念，也不是在没有真实环境时硬讲模型效果。
- 它和 Day05 的区别：
  - Day05 更像“把方案做成能运行的系统”；
  - Day06 更像“第一次用真实评测去验证这个系统到底表现怎样”。
- 放进当前项目里怎么理解：
  - `AI Compliance Check Assistant` 到 Day06 才第一次真正进入“评测驱动”的状态。

---

## 明确的下一步
1. 用户自己在本地终端设置：

```bash
export ANTHROPIC_API_KEY="你的key"
```

2. 重新运行：

```bash
cd 03-学习成果/demo
python3 run_eval.py
```

3. 把以下内容补进这张卡：
- 完整基线分数
- 失败 case
- 准备先改哪条规则
- 改前后分数是否变化

---

## 明日回忆提示
明天继续前，先回答这 3 句：
- Day06 真正验证的是什么？
- 现在卡住的是代码问题还是环境前置条件问题？
- 拿到真实 key 之后，第一步应该跑什么？
