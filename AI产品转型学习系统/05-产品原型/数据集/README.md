# 数据集

评测用的数据集统一放这里。**这个文件夹是"规则执行"模块的数据源：往里丢一个 CSV，网页上的数据集下拉框里就会自动多一个选项，不用改任何代码。**

以前跑大样本要在网页上手填一长串绝对路径（`/Users/li/Downloads/300 case...csv`），换台机器或者文件挪个位置就失效了。改成扫描固定文件夹之后，数据集本身也变成了产品资产的一部分，跟着 git 一起走。

---

## 当前有哪些

| 文件 | 条数 | 覆盖类别 | 来源 / 用途 |
|---|---|---|---|
| `csv50-医药CRM合规评测集.csv` | 50 | 完整 10 类（01-10） | Day03 导入的原始评测框架配套数据集。**覆盖完整10类，所以它测的是"整个产品设想还差多少"**，demo 只实现了6类，另外4类（02/04/08/09）必然miss，这是已知的、主动收敛的范围，不是bug |
| `200case-医药CRM合规压力测试集.csv` | 200 | 5 类（01/03/05/07/10） | Day07 用的大样本压力测试集。**只覆盖已实现类别里的5类，所以它测的是"已经做了的规则到底稳不稳"** |
| `300case-Gemini生成合规压力测试集.csv` | 300 | 完整 10 类（01-10），各类都有覆盖 | 2026-08-09 从 Gemini notebook 迁入。**三个数据集里样本量最大、类别覆盖最全的一个**，Critical/Medium/Low 三档风险等级都有覆盖（139/64/97条），既能测"6类规则稳不稳"也能测"离完整10类还差多远"，是目前最接近生产环境分布的一份。⚠️ 注意：`分类ID` 列写的是**旧的10类编号**，跟当前 v3.0 规则的 01-04 新编号不是一回事，按类别拆解这张表在这份数据集上目前是错位的，先别信它——只看 Release Gate 指标（Critical Recall / Medium Precision / Low FP Rate）是准的 |
| `300case-简化二分类版-20260809.csv` | 300 | 无分类标注，只有风险等级+处置动作 | 用户在 `300case-Gemini生成合规压力测试集.csv` 基础上手动简化：删掉了分类列，判断等级也收敛成两档（Critical→Reject 203条，Low→Pass 97条）。**这份数据集刻意规避了"新旧分类编号对不上"的问题**，只测最核心的问题："这段文本到底该不该被拦下来"，不纠结具体是哪一类。跑起来会自动跳过"按类别拆解"这张表，只看 Release Gate 指标 |

另外还有一个内置的 **MVP子集**（30条，`../规则执行/eval_cases.json`），是自己手写的小规模冒烟测试集，包含违规/边界/near-miss/兜底四种类型，跑得快、适合改完规则立刻验证一遍。它不在这个文件夹里，因为它是 JSON 不是 CSV，但网页上照样能选。

### 几个数据集分别回答什么问题

选哪个不是"越大越好"，是看你这次想验证什么：

- **MVP子集（30条）**：刚改完规则，想立刻知道有没有改坏 → 一分钟跑完
- **200case（200条）**：想知道当前这6类规则在大样本下稳不稳、边界样本会不会误伤 → 中等规模回归集
- **300case（300条）**：数据量最大、覆盖最全，但目前分类编号跟新规则版本对不上，**只看 Release Gate 指标，不看按类别拆解**
- **300case简化二分类版（300条）**：2026-08-09规则改成4大类之后，**当前建议的主力压力测试集**——没有分类编号错位的包袱，专注测"该拦的拦住了没、不该拦的误伤了没"
- **CSV50（50条）**：想快速看一眼离"完整10类产品"还差多远 → 拿来讲产品路线图，条数少跑得快，但样本量不如300case有代表性

---

## 表头兼容：不同数据集列名不统一怎么办

这两个数据集的表头其实对不上：

| 内部字段 | csv50 用的列名 | 200case 用的列名 |
|---|---|---|
| case编号 | `Case ID` | `ID` |
| 风险等级 | `风险等级` | `风险级别` |
| 预期动作 | `预期处置动作` | （没有这一列） |

以前 `run_eval_csv.py` 写死了 csv50 的列名，直接喂 200case 会 `KeyError` 崩掉。现在 `../规则执行/datasets.py` 里维护了一张列名别名表做归一化，两种表头都能读；缺失的列（比如 200case 没有预期动作）按"不比对这一项"处理，而不是报错。

要加新数据集，只要表头里能认出**分类ID**和**拜访记录文本**这两列就能跑（其余列缺了会用规则配置里的默认值兜底）。别名表在 `datasets.py` 的 `COLUMN_ALIASES`，加一个新叫法进去就行。

---

## 数据质量检查

加载数据集时会自动检查分类ID是否在 01-10 的体系内，有问题会在网页上提示。注意区分两种情况：

- **02 / 04 / 08 / 09**：不报错。这是"已知未实现"的类别，demo 主动收敛掉的范围，跑分时自然会miss，属于预期内
- **不在 01-10 内的ID**：会报数据质量问题。这是真的录入错了

### 已修正的数据问题

- `200case-医药CRM合规压力测试集.csv` 的 `122` 号case，`分类ID` 原本写的是 `107`，但它的风险分类列写的是"夸大疗效与绝对化宣讲"，明显是 `07` 多打了一个字符。已在这份迁移副本里改成 `07`。仓库根目录的原始文件 `pharma_crm_compliance_eval_200cases.csv` 保持原样没动，可以对照。
- `300case-Gemini生成合规压力测试集.csv` 迁入时跑过同样的质量检查，**0条数据质量问题**，`分类ID` 全部落在 01-10 体系内，不用改。
- `300case-简化二分类版-20260809.csv` 没有分类列，跑质量检查也是 **0条问题**（没有分类ID可查，自然不会报错）。这份数据集依赖 `../规则执行/datasets.py` 2026-08-09新加的"分类列可选"能力：没有分类标注时不会报错，评测时自动跳过按类别拆解，只看风险等级/处置动作判得对不对。
